package rp.assignments.team.warehouse.server.communications;

import java.io.DataInputStream;
import java.io.DataOutputStream;
import java.io.InputStream;

/**
 * Test class which imitates the {@link CommunicationsManager} server side
 * communications class, but uses sockets to create the streams rather than an
 * NXT bluetooth object.
 */
public class TestServer {
	
}
